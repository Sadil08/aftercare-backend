# ──── Production overrides ────
# Run with: java -Dspring.profiles.active=prod -jar aftercare-portal.jar
# Set environment variable: export JWT_SECRET=<minimum-256-bit-secret>

spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

app.jwt.secret=${JWT_SECRET}

logging.level.com.aftercare=INFO
